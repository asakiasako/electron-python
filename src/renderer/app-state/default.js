// initial value
let appState = {
  // example appState, delete them.
  app: {
    isFullScreen: false,
    windowContentSize: [1280, 720]
  },
  trx: {
    sn: null,
    connected: false
  }
}

export default appState
